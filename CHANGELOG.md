# 更新记录

本文档记录 `viz_app` 已新增或变更的功能，供后续开发、回归验证和问题定位使用。

## 维护约定

- 按日期倒序记录，最新变更放在最上方。
- 每次新增功能、修改行为或修复问题时，必须在同一次提交中更新本文档。
- 重点描述用户可见行为、数据约定和关键兼容性，不记录无行为变化的格式调整。
- 每条历史记录保留 Git 短哈希，便于查看对应实现和完整差异。

## 2026-09-21

- 左侧图片列表仅显示文件名（含扩展名），兼容 Windows 和正斜杠路径；悬停保留完整路径，图片加载、筛选及导出仍使用原始路径。
- Objects 列表每项目标占用高度由 38px 减半为 19px，同步缩小行内按钮，保留字号及左侧图片列表行高。
- 取消左栏切图时重复显示的加载路径和“图片已加载”提示，正常加载完成后状态行不占空间；保留数据处理进度、操作反馈与加载错误提示。
- 图片信息水印由画布左下角移至左上角，保留置顶显示、鼠标穿透及固定视口位置。
- 文件选择保留原生交互，改用独立居中显示的按钮和文件名，避免系统按钮文字裁切；尺寸统计缩写为“尺寸已知”，与确定、清空、结果数保持单行，完整说明通过悬停提示查看。
- 将序号、宽高、Objects、缩放和创建时间移至画布左下角置顶水印，单图及批量预览均显示，缩放平移时位置固定且不拦截鼠标；移除顶部信息行，将释放高度分配给画布。
- 修复紧凑控件的文字垂直对齐，统一按钮和单行输入行高，调整原生文件选择按钮与文件名基线；右侧新增矩形框、新增四点框、删除选中使用 11px 字号并排成一行；两侧面板及列表取消圆角，面板间不留缝隙。
- 左右侧栏紧凑化：压缩导入、筛选、对象操作、坐标和关键点控件及区块留白，保留字号和虚拟列表行高，将更多空间分配给图片及 Objects 列表；labels 编辑框默认 60px 且可拉高，编辑区内容过多时滚动。
- 压缩画布上方工具栏：图片名、序号、批量数量及操作按钮高度统一为 22px，全部复位按钮为 20px，缩小行间距及上下留白；保留字号、滑块操作区域及左右面板尺寸。
- 中间画布与工具栏、左右面板及底部贴合，移除布局缝隙、画布圆角与阴影，保留分隔边框及图片等比例显示。
- 图像调整独立成行，亮度和对比度保持同组；滑块圆点缩小，保留点击区域和键盘焦点提示。
- 新增饱和度、灰度及锐化滑块，支持点击名称单项复位和全部复位；锐化默认关闭，仅影响底图显示，不修改原图、标注或 JSON。
- 修正亮度、对比度拖到 0% 时错误恢复为 100% 的行为，新增滤镜组合、锐化核和参数边界测试。

## 2026-09-20

- `5a0445c` 在原“筛选 labels”输入框中加入候选搜索：输入字符时展示包含该字符的 labels，支持多选、取消和与下方多选框同步；没有选中候选时继续使用原有模糊筛选。下方多选框支持 `Esc` 收起，并在每次重新展开时回到列表顶部。
- `b477664` 将 labels 下拉框由单选升级为多选，多个 labels 按 OR 规则筛选；已选项高亮并置顶，取消后恢复原始顺序；“负样本”可与普通 label 组合选择。
- `157fd76` 在批量预览中绘制标注框，并复用“显示框”“框填充”等显示控制；按图片实际 contain 缩放位置同步绘制标注。
- `5d5df6b` 新增 `Esc` 快捷键，用于取消当前标注对象的选中状态。
- `269763d` 修复 labels 筛选后的“显示所有框”行为：开启时当前画布临时绕过 labels 过滤并显示该图全部标注，关闭后恢复过滤规则。
- `46b7a51` labels 筛选增加“负样本（无 labels）”，用于筛选没有任何有效 label 的图片。

## 2026-09-19

- `281390e` 新增分类 JSON 批量更新脚本：按图片文件名匹配分类目录，更新 JSON 键名与 labels，移除不存在的图片记录；检测跨分类重名，并以 UTF-8 保留中文；配套自动化测试。

## 2026-09-18

- `27014ef` 完善标注编辑与快捷操作：矩形拖动角点或边时始终保持轴对齐矩形；标注点、边和整体移动均限制在图片边界内；支持 `Ctrl+Z` 撤销、`Delete` 删除选中标注、WASD/方向键切换图片；鼠标接近选中标注的点、边或框体时显示对应光标和悬停反馈。

## 2026-09-17

- `86bbfb2` 新增可配置批量预览：用户可输入 `N` 生成 `N x N` 图片网格，支持范围归一化、分页窗口、进度定位和滚轮切换，并保证末页尽量填满网格。
- `5bf15fb` 新增与原查看器隔离的现场缺陷项目工作流：支持项目创建与历史记录、图片导入和去重、目录递归扫描、剪贴板图片、Excel 内嵌图片、车型相机对比图、缺陷多选筛选、未标注筛选、排序、标注维护及项目目录持久化；增加共享应用切换入口和完整核心测试。

## 2026-09-14

- `614f9a9` 新增 label 感知的标注框过滤控制，使图片筛选和画布标注显示可以依据 labels 联动。
- `a41b5f4` 优化标注编辑界面：筛选条件改为点击“确定”后统一生效；支持勾选图片并导出对应图片与编辑后的 JSON；移除可见 JSON 文本编辑区，压缩图片列表行高并调整筛选区布局。

## 2026-07-30

- `36c8f1a` 新增图片显示参数复位控制，可恢复亮度、对比度等图像调整参数，并优化相关控件布局。
- `ff71060` 新增图片对比度调节控件，并将对比度效果接入画布显示流程。

## 2026-07-28

- `0ca4bbf` 区分矩形框与四点框的创建和编辑逻辑，为不同框型提供独立操作入口及对应数据标记。

## 2026-07-27

- `0bbd12a` 支持在图片画布上点击，为当前标注直接放置或更新关键点。
- `b224400` 新增关键点编辑控件，包括关键点选择、坐标编辑、新增和应用修改，并同步画布与编辑面板状态。

## 2026-07-21

- `1350dd4` 修复关键点数据存在空槽位或缺失位置时的解析与显示问题，避免关键点序号错位。

## 2026-07-08

- `32cf6f2` 新增关键点覆盖层显示控制，将关键点数据解析、画布绘制和显隐开关联动。

## 2026-07-07

- `8304ff6` 收紧查看器整体布局，提升有限屏幕空间下的信息密度，并修复界面中文显示问题。

## 2026-06-12

- `a3df1ac` 新增图片亮度滑块，并将亮度参数应用到当前图片显示。
- `a6993cf` 重构查看器主流程：统一入口为 `index.html`，新增 `json-worker.js` 在后台解析和校验大 JSON，重整应用状态、响应式布局和列表渲染，为后续大数据量查看与编辑奠定基础。

## 2026-06-05

- `99a983d` 新增图片元数据和目标尺寸显示，包括图片序号、宽高、对象数量及标注框尺寸信息。

## 2026-06-04

- `6140892` 提升查看器响应式布局与当前图片信息展示，优化不同窗口尺寸下的画布和侧栏行为。

## 2026-06-03

- `83f45f0` 优化图片切换性能，引入图片预加载和更高效的列表更新，减少切换过程中的重复渲染。
- `af3b36a` 完善图片查看控制：改进缩放、平移、上一张/下一张切换及画布操作区域，并调整查看器布局。

## 2026-06-02

- `11c6e46` 将应用入口文件由 `index.html` 更名为 `ty_viz.html`，并同步更新使用说明；该入口在后续重构中恢复为 `index.html`。
- `0b5e916` 将 Ming 标识从独立徽标移入副标题，简化顶部品牌区域。
- `c369172` 调整应用名称并新增 Ming 标识，更新页面标题和品牌样式。
- `5bfd85d` 修复点击画布标注框后对象列表未正确同步选中的问题。
- `7349b3b` 增加画布对象与右侧对象列表的双向选中同步，并支持调整标注 label 字号。
- `dfa389b` 优化标注覆盖层渲染，减少缩放、平移和交互过程中的重复计算与绘制开销。
- `e6059c4` 新增标注框线宽调节控件，允许实时调整覆盖层描边粗细。
- `3bdeb22` 改进画布平移交互，并增加标注框填充显示开关。
- `5e622ca` 新增画布缩放与平移，支持围绕图片浏览局部细节。
- `4cfee31` 创建初始数据可视化工具：支持导入标注 JSON、按图片浏览数据、绘制标注框、查看和编辑对象信息以及导出修改后的 JSON。
